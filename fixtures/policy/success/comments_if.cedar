permit (principal, action,resource)
when {
( //comment open outer
( //comment open inner
 if //comment if
  1             //comment
  < //comment <
  2 //comment 2
  then // comment then
  "hello" //comment hello
else  //comment else
    1 //comment 1
    ) //comment close inner
    ) //comment close outer
};
